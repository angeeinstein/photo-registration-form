# Environment Variables
# Copy this file to .env and update with your values

# ==============================================================================
# REQUIRED SETTINGS - Must be configured before running
# ==============================================================================

# Flask Secret Key (REQUIRED - for session encryption)
# Generate a secure random key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-me-in-production

# Admin Panel Credentials (REQUIRED - for admin login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-in-production

# ==============================================================================
# OPTIONAL SETTINGS - Have sensible defaults
# ==============================================================================

# Database Configuration
# Default: SQLite database in the application directory
# Uncomment to use PostgreSQL or change location:
# DATABASE_URI=sqlite:///registrations.db
# DATABASE_URI=postgresql://user:password@localhost/photo_registration

# Gunicorn Server Binding
# Default: 127.0.0.1:5000 (localhost only)
# For network access (e.g., with Cloudflare Tunnel): 0.0.0.0:5000
# For port 80 network access (requires root/privileges): 0.0.0.0:80
# GUNICORN_BIND=127.0.0.1:5000

# ==============================================================================
# EMAIL CONFIGURATION - Now Managed in Database (via Admin Panel)
# ==============================================================================
# ⚠️ Email accounts are now configured through the Admin Panel web interface
# Navigate to: Admin Dashboard → Email Accounts → Add Email Account
# 
# The settings below are OPTIONAL and only used for automatic migration:
# - On first run, if these variables are set, they will be automatically 
#   migrated to the database as your first email account
# - After migration, manage all email accounts through the Admin Panel
# - You can safely remove these after migration is complete

# SMTP Server Settings (OPTIONAL - for automatic migration only)
# Uncomment to enable automatic migration to database on first run:
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false
# SMTP_FROM_EMAIL=your-email@gmail.com
# SMTP_FROM_NAME=Photo Registration

# ==============================================================================
# EMAIL BEHAVIOR SETTINGS
# ==============================================================================

# Automatic Confirmation Emails
# Set to 'true' to automatically send confirmation emails when someone registers
# Set to 'false' to disable automatic confirmations (can still send manually)
# Default: true
SEND_CONFIRMATION_EMAIL=true

# Email Subject Lines
# Customize the subject lines for automated emails
CONFIRMATION_EMAIL_SUBJECT=Registration Confirmation - Thank You!
PHOTOS_EMAIL_SUBJECT=Your Event Photos Are Ready!

# ==============================================================================
# REMOVED/UNUSED SETTINGS
# ==============================================================================
# The following variables are NO LONGER USED and can be removed:
# 
# - FLASK_ENV (deprecated in Flask 2.3+, use --debug flag instead)
# - FLASK_APP (not needed when using gunicorn or direct python execution)
# - WORKERS (gunicorn calculates this automatically based on CPU cores)
# - TEST_EMAIL (no longer used, test emails now use custom recipient in UI)
# - ADMIN_SESSION_SECRET (uses SECRET_KEY instead for consistency)
